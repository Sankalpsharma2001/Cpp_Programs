#include<bits/stdc++.h>
using namespace std;
void quick(char nuts[],char bolts[],int low,int high)
{
    if(low<high)
    {
        
    }
}
int main()
{
char nuts[]={'@', '#', '$', '%', '^', '&'};
char bolts[] = {'$', '%', '&', '^', '@', '#'};
quick(nuts,bolts,0,5);

return 0;
}